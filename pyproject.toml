[project]
name = "swarmui-quarryextension"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "duckdb>=1.5.3",
    "pip>=26.1.2",
    "pylance>=7.0.0",
]
